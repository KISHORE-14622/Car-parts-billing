{
  "name": "car-parts-barcode-scanner",
  "version": "1.0.0",
  "description": "Car parts seller web application with barcode scanning functionality",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["car-parts", "barcode-scanner", "react", "express", "mongodb"],
  "author": "Car Parts Seller",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
